# zent7x.com

Personal site for [zentex](https://zent7x.com) — static HTML, no framework.

```bash
npm run build   # copies index.html, site.js, public/ → dist/
```

GitHub Actions deploys `dist/` to Pages. Custom domain: `zent7x.com`.
